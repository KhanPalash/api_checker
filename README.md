# api_checker
KhansCreations API Checker is a clean, mobile-friendly web app that quickly tests whether an API endpoint is live and valid. Just select a base URL (or use custom), add path + token, auto-detect/select model (or type manually), and get instant status, latency, and response insight with clear success/error UI.
